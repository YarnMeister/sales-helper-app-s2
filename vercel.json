{
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "buildCommand": "npm run db:migrate && npm run build:debug",
  "installCommand": "npm install",
  "framework": "nextjs",
  "ignoreCommand": "echo 'Preview deployments disabled' && exit 1",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "regions": ["iad1"],
  "public": false
}
